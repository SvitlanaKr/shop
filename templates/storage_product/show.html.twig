{% extends 'base.html.twig' %}

{% block title %}StorageProduct{% endblock %}

{% block body %}
    <h1>StorageProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ storage_product.id }}</td>
            </tr>
            <tr>
                <th>Count</th>
                <td>{{ storage_product.count }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('storage_product_index') }}">back to list</a>

    <a href="{{ path('storage_product_edit', {'id': storage_product.id}) }}">edit</a>

    {{ include('storage_product/_delete_form.html.twig') }}
{% endblock %}
